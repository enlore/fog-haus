{
  "name": "fog.haus",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm-run-all --parallel html styles serve",
    "html": "pug -O data.json --pretty --watch index.pug -o .",
    "styles": "stylus --watch main.styl",
    "serve": "python -m SimpleHTTPServer 3000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^3.1.0",
    "pug": "^2.0.0-beta6",
    "stylus": "^0.54.5"
  }
}
